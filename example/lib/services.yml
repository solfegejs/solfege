services:
    example_configuration:
        class: "Command/ConfigurationCommand"
        arguments:
            - "%parameters.foo%"
            - "%parameters.port%"
            - "%routing%"
            - "@solfege_configuration"
        calls:
            - [addSomething, ["@solfege_configuration", "%parameters.foo%"]]
        tags:
            - { name: "solfege.console.command" }
