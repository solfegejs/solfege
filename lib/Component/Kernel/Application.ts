/**
 * Main class of the application
 */
class Application
{
    /**
     * Constructor
     *
     * @param   {string}    environment     The environment of the runtime
     * @param   {boolean}   debug           Indicates that the debug mode is enabled
     */
    constructor(environment:string, debug:boolean = false)
    {
        
    }
}

export = Application;